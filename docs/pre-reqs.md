# Pre-requisitos

## Ferramentas

- `helm` v3+
- `kubectl`
- acesso a cluster Kubernetes (preferencial: `k3s`)

## Conhecimento mínimo esperado

- basico de Kubernetes (pod, deployment, service, namespace)
- comandos de terminal

## Validacao rapida

```bash
helm version
kubectl version --client
kubectl get nodes
```
